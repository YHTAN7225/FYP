@*@model IEnumerable<SignatureRequest>

@{
    ViewData["Title"] = "Sign";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Sign</h1>
<p>
    <a asp-action="RequestSignature">Create new sign request</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                User Id
            </th>
            <th>
                User Email
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserAccess.UserId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @Html.ActionLink("Delete User", "DeleteUser", new { UserId = item.UserAccess.UserId })
                </td>
            </tr>
        }
    </tbody>
</table>*@